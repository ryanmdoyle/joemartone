import { css } from '@emotion/core';

export const pageListStyles = css`
  h4 { 
    font-size: 1.2rem;
    color: #2f2f2f;
    text-transform: uppercase;
  }
  ul {
    color: #2f2f2f;
    letter-spacing: 1px;
  }
`;

export default pageListStyles;